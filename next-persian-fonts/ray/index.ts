import localFont from "next/font/local";

export const ray = localFont({ src: "./Ray.ttf", variable: "--font-ray" });
export const rayBold = localFont({ src: "./Ray-Bold.ttf", variable: "--font-ray" });
